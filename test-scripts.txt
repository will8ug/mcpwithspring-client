curl --location 'http://localhost:8081/ai/chat' \
--header 'Content-Type: application/json' \
--data '{
    "question": "Tell me a joke"
}'

curl --location 'http://localhost:8081/ai/chat' \
--header 'Content-Type: application/json' \
--data '{
    "question": "What is the capital of France?"
}'

curl --location 'http://localhost:8081/ai/chat' \
--header 'Content-Type: application/json' \
--data '{
    "question": "What tools are available?"
}'

curl --location 'http://localhost:8081/ai/chat' \
--header 'Content-Type: application/json' \
--data '{
    "question": "What is the weather in Guangzhou?"
}'

curl -X POST --location 'http://localhost:8081/mcp/examine-tools'
