###################### Basic

curl --location 'http://localhost:8081/ai/chat' \
--header 'Content-Type: application/json' \
--data '{
    "question": "Tell me a joke"
}'

curl --location 'http://localhost:8081/ai/chat' \
--header 'Content-Type: application/json' \
--data '{
    "question": "What is the capital of France?"
}'

###################### MCP

curl --location 'http://localhost:8081/ai/chat' \
--header 'Content-Type: application/json' \
--data '{
    "question": "What tools are available?"
}'

curl --location 'http://localhost:8081/ai/chat' \
--header 'Content-Type: application/json' \
--data '{
    "question": "What is the weather in Guangzhou?"
}'

curl -X POST --location 'http://localhost:8081/mcp/examine-tools'
curl --location 'http://localhost:8081/mcp/clients/count'

curl --location 'http://localhost:8081/ai/chat' \
--header 'Content-Type: application/json' \
--data '{
    "question": "List the files in my home directory"
}'

curl --location 'http://localhost:8081/ai/chat' \
--header 'Content-Type: application/json' \
--data '{
    "question": "List the files in current directory"
}'

curl --location 'http://localhost:8081/ai/chat' \
--header 'Content-Type: application/json' \
--data '{
    "question": "What is the current working directory?"
}'

curl --location 'http://localhost:8081/ai/chat' \
--header 'Content-Type: application/json' \
--data '{
    "question": "What are the hot news in Weibo today?"
}'

################# Chat Memory
curl -X POST http://localhost:8081/ai/chat-in-conversation \
  -H "Content-Type: application/json" \
  -d '{
    "question": "Hello, how are you?",
    "conversationId": "test-conversation-001"
  }'

curl -X POST http://localhost:8081/ai/chat-in-conversation \
  -H "Content-Type: application/json" \
  -d '{
    "question": "My name is Will",
    "conversationId": "test-conversation-001"
  }'

curl -X POST http://localhost:8081/ai/chat-in-conversation \
  -H "Content-Type: application/json" \
  -d '{
    "question": "Do you remember my name?",
    "conversationId": "test-conversation-001"
  }'

curl -X POST http://localhost:8081/ai/chat-in-conversation \
  -H "Content-Type: application/json" \
  -d '{
    "question": "Let us have one more round of test.",
    "conversationId": "test-conversation-001"
  }'

curl -X POST http://localhost:8081/ai/chat-in-conversation \
  -H "Content-Type: application/json" \
  -d '{
    "question": "What do we have now?",
    "conversationId": "test-conversation-001"
  }'

